--- a/Hammerspoon 2/Modules/hs.ipc/HSIPCServer.swift	
+++ b/Hammerspoon 2/Modules/hs.ipc/HSIPCServer.swift	
@@ -11,7 +11,7 @@
 
 @safe @MainActor
 final class HSIPCServer: NSObject {
-    static let serviceName = "net.tenshu.Hammerspoon-2.ipc"
+    static let serviceName = (Bundle.main.bundleIdentifier ?? "net.tenshu.Hammerspoon-2") + ".ipc"
 
     private(set) var isListening = false
 
--- a/Hammerspoon 2/Modules/hs.urlevent/HSURLEventModule.swift	
+++ b/Hammerspoon 2/Modules/hs.urlevent/HSURLEventModule.swift	
@@ -216,6 +216,13 @@
 
     // MARK: - Internal URL dispatch
 
+    // Use the app's declared custom scheme so embedded distributions can keep
+    // URL dispatch and Console commands in their own namespace.
+    static var customScheme: String {
+        let types = Bundle.main.object(forInfoDictionaryKey: "CFBundleURLTypes") as? [[String: Any]]
+        return (types?.first?["CFBundleURLSchemes"] as? [String])?.first ?? "hammerspoon2"
+    }
+
     func handleURL(_ url: URL) {
         let scheme = url.scheme ?? ""
         let fullURL = url.absoluteString
@@ -223,7 +230,7 @@
         let senderPID = -1
 
         switch scheme {
-        case "hammerspoon2":
+        case Self.customScheme:
             let eventName = url.host ?? ""
             if let cb = bindings[eventName] {
                 _ = cb.call(withArguments: [eventName, params, senderPID, fullURL])
--- a/Hammerspoon 2/Engine/ModuleRoot.swift	
+++ b/Hammerspoon 2/Engine/ModuleRoot.swift	
@@ -199,13 +199,13 @@
 
     // MARK: - Console
     @objc func openConsole() {
-        if let url = URL(string:"hammerspoon2://openConsole") {
+        if let url = URL(string:"\(HSURLEventModule.customScheme)://openConsole") {
             NSWorkspace.shared.open(url)
         }
     }
 
     @objc func closeConsole() {
-        if let url = URL(string:"hammerspoon2://closeConsole") {
+        if let url = URL(string:"\(HSURLEventModule.customScheme)://closeConsole") {
             NSWorkspace.shared.open(url)
         }
     }
--- a/HammerspoonOSAScriptHelper/HSOSAScriptShared.swift	
+++ b/HammerspoonOSAScriptHelper/HSOSAScriptShared.swift	
@@ -8,8 +8,15 @@
 // This is for data that needs to be shared between the HammerspoonOSAScriptHelper XPC helper, and Hammerspoon itself
 // This file is compiled into both binaries.
 
-let HSOSAScriptServiceName = "net.tenshu.Hammerspoon-2.HammerspoonOSAScriptHelper"
+import Foundation
 
+let HSOSAScriptServiceName: String = {
+    let suffix = ".HammerspoonOSAScriptHelper"
+    let identifier = Bundle.main.bundleIdentifier ?? "net.tenshu.Hammerspoon-2"
+    // This source is compiled into both the containing app and the XPC helper.
+    return identifier.hasSuffix(suffix) ? identifier : identifier + suffix
+}()
+
 nonisolated struct HSOSARequest: Codable {
     let language: String
     let source: String
--- a/hs2/main.swift	
+++ b/hs2/main.swift	
@@ -99,7 +99,16 @@
     private let connection: NSXPCConnection
 
     init() {
-        let c = NSXPCConnection(machServiceName: "net.tenshu.Hammerspoon-2.ipc")
+        // installBinary() creates a symlink. Resolve the running executable to
+        // find the containing app even when invoked through that symlink.
+        let appURL = Bundle.main.executableURL?.resolvingSymlinksInPath()
+            .deletingLastPathComponent().deletingLastPathComponent().deletingLastPathComponent()
+        // The CLI embeds its own Info.plist. Bundle(url:) can reuse that main
+        // bundle, so read the containing application's metadata from disk.
+        let plist = appURL.flatMap { try? Data(contentsOf: $0.appendingPathComponent("Contents/Info.plist")) }
+            .flatMap { try? PropertyListSerialization.propertyList(from: $0, format: nil) as? [String: Any] }
+        let identifier = plist?["CFBundleIdentifier"] as? String ?? "net.tenshu.Hammerspoon-2"
+        let c = NSXPCConnection(machServiceName: identifier + ".ipc")
         c.remoteObjectInterface = NSXPCInterface(with: HSIPCServerProtocol.self)
         c.exportedInterface = NSXPCInterface(with: HSIPCClientProtocol.self)
         c.exportedObject = HSIPCLogDelegate()

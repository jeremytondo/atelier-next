# Selected explicitly by CI's Linux job; the omitted native tools have no Linux builds.
[tools]
node = "26.4.0"
gh = "2.95.0"
jq = "1.8.1"
shellcheck = "0.11.0"
actionlint = "1.7.12"
biome = "2.5.13"

[task_config]
includes = ["mise/tasks.toml"]
